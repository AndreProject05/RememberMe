<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/pensa"
        android:label="@string/app_name"
        android:theme="@style/Theme.RememberMe"
        tools:targetApi="31">

        <!-- MainActivity con android:exported esplicito -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SummaryActivity con android:exported esplicito -->
        <activity
            android:name=".SummaryActivity"
            android:exported="false" />

        <!-- BootReceiver con android:exported esplicito -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- DeleteListReceiver per eliminare liste dalle notifiche -->
        <receiver
            android:name=".DeleteListReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
